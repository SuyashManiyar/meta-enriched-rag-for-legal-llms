# Data Paths Configuration

data:
  raw: "data/raw"
  cleaned: "data/cleaned"
  chunks: "data/chunks"
  embeddings: "data/embeddings"
  index: "data/index"

corpus:
  maud: "../maud"

output:
  logs: "logs"
  results: "results"
  
models:
  cache_dir: "models"
